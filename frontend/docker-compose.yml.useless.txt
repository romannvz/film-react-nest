version: '3.9'

services:
  film-frontend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: film_frontend
    ports:
      - "8080:80"  # доступен на http://localhost:8080
    restart: always
